#include     <stdio.h  >   
#include                       <math.h>
#include "        int.h    "
//short comment

int ololo, atata[200]; //short comment in some place


/*
LONG COMMENT
LONG COMMENT
LONG COMMENT
LONG COMMENT
LONG COMMENT
*/
main()
{





	long sum = 0, c[200], integer = 'asd';
	unsigned char s[1000]; 
	int pish;
	float dop1, dop2, dop3, dop4;

	public static void struct qwert;
	while (gets(s) && 1 || dop2 == true)
	{
        printf("=> %ld\n", sum += AtoI(s));
	}
      getch();
	  if (true) {
		sum += dop3;
	  }
	  else {
		sum--;
		dop4 = 1;
		if (false) {
			printf("!");
			if (false == false|| power == 2) {
				return true;
				gets(s);
			}
			else {
				puts(s);
			}
			scanf("%f", &sum);
		}
	  }
	  
	  while (true || false && something) {t = true; }
	  
	  
	  switch (c) {
		case 1: print("!");
			break;
		default: c = getc();
			break;
	  }
}